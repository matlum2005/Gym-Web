Place your images in the `frontend/assets` folder with these exact filenames:

- `gym-image.jpg` — background hero image (copy from `C:\Users\DELL\Downloads\GYM iamge.jpg`)
- `logo-gym.jpg` — site logo (copy from `C:\Users\DELL\Downloads\logo gym.jpg`)

To run frontend locally, you can open `frontend/index.html` in a browser or serve the folder with a static server:

```bash
# using simple python http server
cd frontend
python -m http.server 8080
```


